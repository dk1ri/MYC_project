The MYC system can be accessed via a terminal but a webinterface is more comfortable.
The pages are automatically created using the announcement data of the devices.
In a later version the user will be able to configure the page, change color, location of the elements etc
